#include "holberton.h"
#include <stdio.h>

/**
 * mul - prints the last digit of a number
 * @a: The number to print
 * @b: The second number
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
